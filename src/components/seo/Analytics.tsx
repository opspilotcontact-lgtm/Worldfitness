import Script from "next/script";
import { GA_ID, META_PIXEL_ID } from "@/lib/site";

/**
 * GA4 + Meta Pixel. Con IDs placeholder no se inyecta nada (evita requests
 * inútiles en la demo). Cuando Miguel ponga los IDs reales en lib/analytics.ts,
 * el tracking se activa solo.
 */
export function Analytics() {
  const gaActive = GA_ID && !GA_ID.includes("PLACEHOLDER");
  const pixelActive = META_PIXEL_ID && !META_PIXEL_ID.includes("PLACEHOLDER");

  return (
    <>
      {gaActive && (
        <>
          <Script
            src={`https://www.googletagmanager.com/gtag/js?id=${GA_ID}`}
            strategy="afterInteractive"
          />
          <Script id="ga4" strategy="afterInteractive">
            {`
              window.dataLayer = window.dataLayer || [];
              function gtag(){dataLayer.push(arguments);}
              gtag('js', new Date());
              gtag('config', '${GA_ID}');
            `}
          </Script>
        </>
      )}

      {pixelActive && (
        <Script id="meta-pixel" strategy="afterInteractive">
          {`
            !function(f,b,e,v,n,t,s)
            {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
            n.callMethod.apply(n,arguments):n.queue.push(arguments)};
            if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
            n.queue=[];t=b.createElement(e);t.async=!0;
            t.src=v;s=b.getElementsByTagName(e)[0];
            s.parentNode.insertBefore(t,s)}(window, document,'script',
            'https://connect.facebook.net/en_US/fbevents.js');
            fbq('init', '${META_PIXEL_ID}');
            fbq('track', 'PageView');
          `}
        </Script>
      )}
    </>
  );
}
